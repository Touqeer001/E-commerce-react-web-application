import DashboardCard from "./DashboardCard";
import "./Dashboard.css";

export default function DashboardCards({ items }) {
  // return (
  //   <div className="stats">
  //     {items.map((item, i) => (
  //       <DashboardCard key={i} label={item.label} value={item.value} />
  //     ))}
  //   </div>
  // );
}
